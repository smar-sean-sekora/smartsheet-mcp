{
  "name": "smartsheet",
  "version": "0.0.1",
  "description": "smartsheet MCP server",
  "type": "module",
  "bin": {
    "smartsheet": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && mcp-build",
    "watch": "tsc --watch",
    "start": "tsc && mcp-build && node dist/index.js",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "mcp-framework": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0",
    "@types/jest": "^29.5.0"
  },
  "engines": {
    "node": ">=18.19.0"
  }
}
